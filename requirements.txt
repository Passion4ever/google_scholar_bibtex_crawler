# 核心(API 版 + 通用)
httpx>=0.27
rapidfuzz>=3.0
bibtexparser>=1.4,<2

# Google Scholar 浏览器版抓取(scholar_browser.py)
# nodriver: CDP 直连,无需 ChromeDriver,不挑 Chrome 版本
nodriver>=0.50
# playwright: 提供与系统隔离的 Chromium(运行 `playwright install chromium`)
playwright>=1.40
